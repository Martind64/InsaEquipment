{% extends 'AppBundle::sidebar.html.twig'%}

{% block body %}
        <!-- Shows the sidebar -->
        {% block sidebar %}{{ parent() }}{% endblock %}


                <div class="col-lg-8">

                    <h1>Update Type</h1>
                </div>

                <div class="col-lg-4">
                    <hr>
                    {{ form_start(Form) }}
                    {{ form_widget(Form) }}
                    {{ form_end(Form) }}
                </div>
            </div>
        </div>


    </div><!-- Wrapper End -->


{% endblock %}